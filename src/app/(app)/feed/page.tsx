import { StubPage } from "@/components/shell/StubPage";

export default function FeedPage() {
  return (
    <StubPage
      title="The feed is not available yet"
      body="The social feed — where you see what people you follow are rating and saving — is planned for a later phase once accounts and follows are in place."
    />
  );
}
